/// 运行时环境标记。
///
/// 多窗口架构下每个窗口引擎独立运行一次 [main]，
/// 通过启动参数区分主引擎与子窗口引擎。
class AppRuntime {
  AppRuntime._();

  /// 当前引擎是否为多窗口子引擎（由 main() 根据启动参数设置）。
  ///
  /// 主引擎负责调度器、热键、托盘等全局职责；
  /// 子窗口仅展示数据，执行类操作需转发回主引擎。
  static bool isChildWindow = false;
}
